#pragma once
#include <SFML/Graphics.hpp>

// ??? One leaderboard record ????????????????????????????????????????????????????
struct LBRecord {
    char playerName[32];
    char gameName[32];
    int  wins;
    int  totalGames;

    LBRecord();
};

// ??? Leaderboard Manager ??????????????????????????????????????????????????????
class Leaderboard {
public:
    static const int MAX_RECORDS = 200;

private:
    LBRecord records[MAX_RECORDS];
    int      count;

    sf::Font font;
    sf::RenderWindow* window;

    // Colors
    sf::Color neonCyan;
    sf::Color neonPink;
    sf::Color neonYellow;
    sf::Color neonGreen;
    sf::Color neonOrange;
    sf::Color darkBg;

    float glowTimer;
    int   scrollOffset;   // how many rows scrolled down

    // Internal helpers (no std libs)
    static int  lbLen(const char* s);
    static void lbCpy(char* d, const char* s);
    static void lbCat(char* d, const char* s);
    static void lbIts(int v, char* buf);
    static bool lbEq(const char* a, const char* b);

    void drawBackground();
    void centeredText(const char* str, float y, unsigned sz,
        sf::Color col, bool bold = false);
    void drawRoundedRect(float x, float y, float w, float h,
        float r, sf::Color fill,
        sf::Color outline, float thick);

public:
    Leaderboard(sf::RenderWindow* win);

    // ?? Required methods (assignment spec) ????????????????????????????????????
    void AddRecord(const char* playerName, const char* gameName,
        int wins, int totalGames);
    void LoadFromFile(const char* filename = "leaderboard.txt");
    void SaveToFile(const char* filename = "leaderboard.txt");
    void SortRecords();                    // sort by wins descending
    void DisplayLeaderboard();              // graphical SFML screen

    // ?? Extra helpers ?????????????????????????????????????????????????????????
    int  GetCount() const { return count; }
    void FilterByGame(const char* gameName,
        LBRecord* out, int& outCount) const;
};