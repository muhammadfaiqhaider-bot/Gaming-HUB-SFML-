#pragma once
#include <SFML/Graphics.hpp>
#include <SFML/Audio.hpp>

enum class TTTState {
    WELCOME,        // Welcome screen with game title
    ENTER_P1,       // Player 1 name input
    ENTER_P2,       // Player 2 name input
    PLAYING,        // Active game
    ROUND_OVER,     // Round finished - play again or finish?
    FINAL_RESULTS   // Final results screen
};

enum class Cell { EMPTY, X, O };

class TicTacToe {
private:
    sf::RenderWindow* window;
    sf::Font          font;

    // ?? State ????????????????????????????????????????????????????????????????
    TTTState state;
    Cell     board[3][3];
    int      currentPlayer;   // 1=X(P1)  2=O(P2)
    int      winner;          // 0=none 1=P1 2=P2 3=draw
    int      totalGames;
    bool     hasWinLine;
    int      winLine[3][2];

    // ?? Player data ??????????????????????????????????????????????????????????
    char p1Name[32];
    char p2Name[32];
    int  p1Wins;
    int  p2Wins;
    int  draws;

    // ?? Name input ???????????????????????????????????????????????????????????
    char inputBuffer[32];
    int  inputLen;

    // ?? Board geometry ???????????????????????????????????????????????????????
    float boardX, boardY, cellSize;

    // ?? Colors ???????????????????????????????????????????????????????????????
    sf::Color neonCyan;
    sf::Color neonPink;
    sf::Color neonYellow;
    sf::Color neonGreen;
    sf::Color darkBg;

    // ?? Shapes ???????????????????????????????????????????????????????????????
    sf::RectangleShape gridLines[4];
    sf::RectangleShape winLineShape;
    sf::RectangleShape scanlines[120];

    float glowTimer;
    float welcomeTimer;

    // ?? Sounds ???????????????????????????????????????????????????????????????
    sf::SoundBuffer bufX, bufO, bufWin;
    sf::Sound       sndX, sndO, sndWin;

    // ?? Internal helpers ?????????????????????????????????????????????????????
    void initScanlines();
    void initGrid();
    void resetBoard();
    void checkWinner();
    void computeWinLine();
    sf::Vector2i getBoardCell(sf::Vector2i mp);

    void drawBackground();
    void drawGrid();
    void drawBoard();
    void drawXAt(float cx, float cy, float size, sf::Color col);
    void drawOAt(float cx, float cy, float radius, sf::Color col);
    void drawScoreBar();
    void drawInputBox(const char* prompt, sf::Color promptColor,
        const char* subInfo = nullptr);
    void centeredText(const char* str, float y, unsigned size,
        sf::Color col, bool bold = false);

    void handleWelcome(sf::Event& e);
    void handleEnterP1(sf::Event& e);
    void handleEnterP2(sf::Event& e);
    void handlePlaying(sf::Event& e);
    void handleRoundOver(sf::Event& e);
    void handleFinalResults(sf::Event& e);

    void drawWelcome();
    void drawEnterName(int player);
    void drawPlaying();
    void drawRoundOver();
    void drawFinalResults();

public:
    TicTacToe(sf::RenderWindow* win);
    void run();

    // For leaderboard integration
    void getResult(char* p1Out, int& p1Wins, int& p2Wins,
        char* p2Out, int& totalGames) const;
};