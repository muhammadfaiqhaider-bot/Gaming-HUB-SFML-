#pragma once
#include <SFML/Graphics.hpp>
#include <SFML/Audio.hpp>

enum class PongState {
    WELCOME,
    ENTER_P1,
    ENTER_P2,
    COUNTDOWN,
    PLAYING,
    POINT_SCORED,
    MATCH_OVER,
    FINAL_RESULTS
};

class Pong {
private:
    sf::RenderWindow* window;
    sf::Font          font;

    PongState state;

    // ?? Player data ??????????????????????????????????????????????????????????
    char p1Name[32];
    char p2Name[32];
    int  p1Score;       // points in current match
    int  p2Score;
    int  p1Wins;        // matches won
    int  p2Wins;
    int  totalMatches;

    // ?? Input ????????????????????????????????????????????????????????????????
    char inputBuffer[32];
    int  inputLen;

    // ?? Ball ?????????????????????????????????????????????????????????????????
    sf::CircleShape    ball;
    float              ballVx, ballVy;
    float              ballSpeed;
    static const float BALL_RADIUS;
    static const float BASE_SPEED;
    static const float SPEED_INC;

    // ?? Paddles ??????????????????????????????????????????????????????????????
    sf::RectangleShape p1Paddle;
    sf::RectangleShape p2Paddle;
    static const float PADDLE_W;
    static const float PADDLE_H;
    static const float PADDLE_SPEED;
    static const float PADDLE_MARGIN;

    // ?? Field ????????????????????????????????????????????????????????????????
    static const float W;
    static const float H;
    static const int   WIN_SCORE;

    // ?? Countdown ????????????????????????????????????????????????????????????
    float countdownTimer;
    int   countdownNum;

    // ?? Point flash ??????????????????????????????????????????????????????????
    float pointFlashTimer;
    int   lastScorer;   // 1 or 2

    // ?? Trail effect ?????????????????????????????????????????????????????????
    static const int TRAIL_LEN = 10;
    sf::Vector2f     trail[TRAIL_LEN];
    int              trailIdx;

    // ?? Colors ???????????????????????????????????????????????????????????????
    sf::Color neonCyan;
    sf::Color neonPink;
    sf::Color neonYellow;
    sf::Color neonGreen;
    sf::Color darkBg;

    float glowTimer;

    // ?? Sounds ???????????????????????????????????????????????????????????????
    sf::SoundBuffer bufHit, bufMiss, bufWin;
    sf::Sound       sndHit, sndMiss, sndWin;

    // ?? Scanlines ????????????????????????????????????????????????????????????
    sf::RectangleShape scanlines[120];

    // ?? Helpers ??????????????????????????????????????????????????????????????
    void initScanlines();
    void resetBall(int serveDir);
    void resetPaddles();
    void resetMatch();
    void startCountdown();

    void drawBackground();
    void drawField();
    void drawBall();
    void drawPaddles();
    void drawScoreBar();
    void drawCountdown();
    void drawInputBox(const char* prompt, sf::Color col, const char* sub = nullptr);
    void centeredText(const char* s, float y, unsigned size, sf::Color col, bool bold = false);

    void handleWelcome(sf::Event& e);
    void handleEnterP1(sf::Event& e);
    void handleEnterP2(sf::Event& e);
    void handlePlaying(float dt);
    void handlePointScored(sf::Event& e);
    void handleMatchOver(sf::Event& e);
    void handleFinalResults(sf::Event& e);

    void drawWelcome();
    void drawEnterName(int player);
    void drawPlaying();
    void drawPointScored();
    void drawMatchOver();
    void drawFinalResults();

public:
    Pong(sf::RenderWindow* win);
    void run();
    void getResult(char* p1Out, int& p1Wins, int& p2Wins,
        char* p2Out, int& totalMatches) const;
};